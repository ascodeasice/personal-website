const ProjectImage = ({ src }) => {
    // TODO: change one image to image slider(multiple images)
    return (
        <img className="h-[50vh] row-span-3" src={src} />
    );
}

export default ProjectImage;